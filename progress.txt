## Phase 3a — Fix multer security vulnerability (2026-03-10)

Task: Upgrade multer to fix high-severity dicer crash vulnerability (GHSA-wm7h-9275-46v2).

Changes:
- multer: 1.4.4-lts.1 → 2.1.1
- @types/multer: 1.4.11 → 2.1.0

API compatibility: Full. controllers.ts uses diskStorage, .single(), and multer.Options —
all still available and type-compatible in v2. No source code changes needed.

Result: npm audit drops from 5 high-severity vulnerabilities to 2 (remaining: mocha/serialize-javascript).
TypeScript type-check: clean. All 12 tests pass.
## Phase 3b — Fix mocha serialize-javascript vulnerability (2026-03-10)

Task: Upgrade mocha and fix high-severity serialize-javascript vulnerability (GHSA-5c6j-r48x-rmvq).

Changes:
- mocha: 10.3.0 → 11.7.5
- Added npm overrides: serialize-javascript ^7.0.3, diff ^8.0.3

Rationale: mocha 11.7.5 still bundles serialize-javascript ^6.0.2 and diff ^7.0.0 (both
vulnerable). npm overrides forces installation of patched versions (7.0.4 and 8.0.3
respectively) for all transitive consumers.

Result: npm audit drops from 2 high-severity vulnerabilities to 0.
All 12 tests pass. TypeScript type-check: clean.

## Phase 4 — Upgrade @types/express 4.x → 5.x (2026-03-10)

Task: Upgrade @types/express from ^4.17.21 to ^5.0.6 for up-to-date Express 5 type definitions.

Changes:
- @types/express: 4.17.21 → 5.0.6

Result: TypeScript type-check: clean. All 12 tests pass.

## Phase 5a — Upgrade uuid 9 → 10 (2026-03-10)

Task: Upgrade uuid to latest CJS-compatible major version.

Changes:
- uuid: 9.0.1 → 10.0.0
- @types/uuid: 9.0.8 → 10.0.0

Note: uuid v11+ dropped CommonJS support ("type": "module"). Since this project
uses module:commonjs in tsconfig, v10 is the maximum safe upgrade. The import
style (import uuid from 'uuid'; uuid.v4()) remains fully compatible with v10.

Result: TypeScript type-check: clean. All 12 tests pass.
## Phase 5b — Upgrade dotenv 16 → 17 (2026-03-10)

Task: Upgrade dotenv to latest major version.

Changes:
- dotenv: 16.4.5 → 17.3.1

API compatibility: Full. Project uses `import dotenv from 'dotenv'` and `dotenv.config()`,
both of which remain unchanged in v17.

Result: TypeScript type-check: clean. All 12 tests pass.
## Phase 5c — Upgrade express 4 → 5 (2026-03-10)

Task: Upgrade express from ^4.18.2 to ^5.2.1.

Changes:
- express: 4.22.1 → 5.2.1
- utils.ts: errorHandler signature updated to include required `next: NextFunction`
  parameter (Express 5 enforces 4-parameter error middleware)

API compatibility: Full. Routes, middleware, and request/response handling are
unchanged. body-parser continues to work as a separate package in Express 5.

Result: TypeScript type-check: clean. All 12 tests pass.
## Phase 5d — Upgrade mongoose 8 → 9 (2026-03-10)

Task: Upgrade mongoose from ^8.1.3 to ^9.2.4.

Changes:
- mongoose: 8.x → 9.2.4
- database.ts: removed deprecated `useNewUrlParser` and `useUnifiedTopology` options
  from `mongoose.connect()` call (removed in mongoose 9; were no-ops in v8)

Compatibility: mongoose-sequence@6.0.1 declares peerDependencies `mongoose: >=5`,
so no plugin upgrade was needed. All other mongoose APIs (Schema, model, queries,
populate, sort, distinct, save, findByIdAndDelete) are fully compatible with v9.

Result: TypeScript type-check: clean. All 12 tests pass.
